{
  "permissions": {
    "allow": [
      "Exec(node)",
      "Exec(npx)",
      "Exec(npm config)",
      "Exec(rm)",
      "Exec(tasklist)",
      "Exec(npm install)",
      "Exec(pnpm)",
      "Exec(curl)",
      "Exec(netstat)",
      "Exec(taskkill)",
      "Exec(ls)",
      "Exec(awk)",
      "Exec(sleep)",
      "Exec(xargs)"
    ]
  }
}